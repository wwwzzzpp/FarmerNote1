name: farmernote_app
description: "FarmerNote Flutter rewrite with 1:1 mini program parity."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  device_calendar: ^4.3.3
  image_picker: ^1.2.0
  shared_preferences: ^2.5.3
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
