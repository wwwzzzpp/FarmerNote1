name: farmernote_app
description: "FarmerNote Flutter rewrite with 1:1 mini program parity."
publish_to: 'none'
version: 1.0.6

environment:
  sdk: ^3.10.3
::
fluwx:
  app_id: "wx_your_open_platform_app_id"
  debug_logging: true
  ios:
    universal_link: "https://your.domain.com/wechat/"

dependencies:
  flutter:
    sdk: flutter
  device_calendar: ^4.3.3
  image_picker: ^1.2.0
  shared_preferences: ^2.5.3
  timezone: ^0.9.4
  http: ^1.6.0
  uuid: ^4.5.3
  path_provider: ^2.1.5
  fluwx: ^5.7.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/crop_plan_catalog.json
